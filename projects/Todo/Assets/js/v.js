//fresh
document.getElementsByTagName("button")[0].addEventListener("click",menu);

function menu() {
    input = 0
    while(input != "quit"){
        input = prompt("Vad vill du modifiera")
    }
    if(input == "new")
}